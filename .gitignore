# Python
__pycache__/
*.py[cod]
.venv*/
venv/
*.egg-info/

# Pretrained SONIC graphs: ~200 MB, re-downloadable from Hugging Face.
# Fetch them with `python -m g1demo.cli download-sonic`.
checkpoints/

# Generated MJCF variants, built from the source models on demand:
#   python -c "from g1demo.sim import nvidia_meshfree_model; nvidia_meshfree_model()"
assets/g1_*.xml

# Run outputs: plots, reports and exported reference clips. Regenerate with
# the documented `python -m g1demo.cli ...` commands. `artifacts/*` rather than
# `artifacts/` so the demo recordings below can be tracked.
artifacts/*
!artifacts/standing.mp4
!artifacts/standing_gain1.0.mp4
!artifacts/standing_montage.png

# Caches kept inside the repo because the sandbox cannot write ~/.cache.
.cache/

# Editor / OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db
